import React from "react";
import { Icon } from "react-native-elements";

export const SearchButton: React.FC = () => {
  return <Icon type="font-awesome" name="search" color="white" size={24} />;
};
